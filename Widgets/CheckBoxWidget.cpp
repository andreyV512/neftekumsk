#include "stdafx.h"
#include "CheckBoxWidget.h"
/*
Button_SetCheck(h, BST_UNCHECKED);
Button_SetCheck(h, BST_CHECKED);
BST_CHECKED == Button_GetCheck(m.hControl);
*/
//extern HINSTANCE hInstance;
/*
void CheckBoxWidget::Init(HWND hOwner, const wchar_t *name)
{
	hWnd = CreateWindow(L"button", name
			,  WS_CHILD | WS_VISIBLE | BS_AUTOCHECKBOX | WS_TABSTOP
				, 0, 0, 0, 0, hOwner, NULL, hInstance, NULL
				);
}
*/